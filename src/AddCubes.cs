using UnityEngine;

public class AddCubes : MonoBehaviour
{
    /*public string blockGrabMesh = "BlockMesh";
    private bool _startMoreCubes;

    private int _totalCubes = 1;



    private void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.CompareTag(blockGrabMesh))
        {
            _totalCubes++;
        }
    }

    private void OnTriggerExit(Collider other)
    {
        
           
        if (other.gameObject.CompareTag(blockGrabMesh))
        {
          
            _totalCubes--; 
            if (!_startMoreCubes)
                _startMoreCubes = true;
            if (_totalCubes <= 1 && _startMoreCubes)
            {
                GameObject.Find("GeneratorCube").GetComponent<GeneratorCubes>().AddCubes();
            }
        }
    }*/
}